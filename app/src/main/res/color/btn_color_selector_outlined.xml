<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@color/very_light_blue" android:state_enabled="false" />
    <!-- Pressed -->
    <item android:color="@color/blue" android:state_pressed="true" />
    <!-- Default -->
    <item android:color="@color/light_blue" />
</selector>