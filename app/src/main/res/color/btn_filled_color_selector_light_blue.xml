<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@color/blue_very_light" android:state_enabled="false" />
    <!-- Pressed -->
    <item android:color="@color/gray_light" android:state_pressed="true" />
    <!-- Default -->
    <item android:color="@color/blue_light" />

</selector>